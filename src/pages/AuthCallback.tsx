/** Minimal route for OAuth popup callback — session handoff is in main.tsx. */
export default function AuthCallback() {
  return (
    <div className="grid min-h-[100dvh] place-items-center bg-deep text-sm text-white/70">
      Even geduld, je wordt ingelogd…
    </div>
  );
}
